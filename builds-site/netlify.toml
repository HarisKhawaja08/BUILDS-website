# Netlify configuration for BUILDS-website
# Adjust the build command and publish directory to match your project setup.

[build]
  command = "npm run build"
  publish = "dist"

# Redirect all routes to index.html (useful for single-page apps)
[[redirects]]
  from = "/**"
  to = "/index.html"
  status = 200
